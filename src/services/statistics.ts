import { DatabasePool, DatabaseTransactionConnection, sql } from 'slonik';
import { z } from 'zod';

const getNormalUserStatistics = async (pool: DatabasePool) => {

};

export {
  getNormalUserStatistics,
};
